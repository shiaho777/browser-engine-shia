# 最优解内核宣言 · The Optimal-Mechanism Kernel

> 一个开源浏览器内核。目标不是"小而美",是 **SOTA**——综合影响力上,超越 Google。

---

## 宣言

我们要成为开源世界浏览器内核品类的 **SOTA**。不是"可读引擎里的第一",是这个品类里**综合影响力的第一**。我们要超越 Google。

我们清楚这句话的重量。Chromium 是三千万行、二十年、国力级的堆叠。在"谁写得多"这条路上,任何人都输。**所以我们根本不走那条路。**

我们只做三件事:**架构级突破、机制级突破、设计级突破。** 别人用十万行实现的,我们问:有没有一个机制,让这十万行变成一千行加一张表?别人每加一个特性就多一道接缝,我们问:有没有一种结构,让接缝从设计上不可能存在?这不是省力——这是**换维度竞争**:我们和它比的不是产出量,是**每一份机制设计撬动的产出量**。

### 凭什么"最少代码做最多事"在理论上成立

因为 **Web 平台的绝大部分,本质是数据,不是逻辑。**

几千个 CSS 属性、上千个 DOM 接口、HTML 的解析规则——它们是规范里写死的**声明式表格**。Chromium 用几百万行手写代码把它们一条条实现了一遍,那是熵。

我们的赌注是:**把平台表达为数据 + 规范驱动的代码生成 + 复用不可约的脏活(V8 / HarfBuzz / FreeType / Skia),手写的只剩"机制内核"。** 当"加一个属性 = 加一行表"时,功能覆盖面**按规范增长**,手写面**按机制增长**——两者解耦。这是"99% 兼容、却只有极小手写面"在数学上唯一可能成立的路径。

我们不和它比谁写得多。我们比:**谁的手写代码是常数级,而覆盖面是规范级。**

---

## 信条

- **平台即数据(Platform-as-Data)。** Web 的绝大部分是规范里的声明式表格,不是逻辑。我们把它表达为数据 + 代码生成。加一个 CSS 属性 = 加一行 + 它的算法。功能覆盖面按规范增长,手写面按机制增长——两者解耦,这是我们碾压性价比的根。
- **凡约定必由机制强制。** 不靠文档,不靠自觉。增量是第一公民,没有手动标脏;几何只有一个真相来源,偷读编译不过;未实现必须大声报错。v0 死于"接缝焊不上",我们把病根写成宪法,由 CI 物理执行。
- **只亲手写体现"读懂 Web"价值的部分。** V8、文本整形、字形栅格化、TLS——人类共同的基础设施,复用它们是智慧不是认输。我们手写的只有内核:DOM 与增量模型、级联、选择器匹配、布局、绘制命令、内核/guest 边界。
- **极致性能,来自机制而非微优化。** 速度不靠手抠热点,靠增量内核让"只重算变化的部分"成为默认、靠索引让匹配亚平方、靠编译器式管线让每阶段可独立优化。性能是架构的副产品。

---

## 北极星

唯一的硬指标,公开、可证伪、每次提交都跑:

> **compat-per-LOC = (官方 WPT 子集通过数) / (人工编写的源代码行数)**

我们诚实承认:生成代码也是代码。我们买到的是**可读、可信、手写面极小**,不是自欺的"系统更小"。分母只算手写行,生成产物不计入手写面,但系统总量报告里照实声明。

---

## 目标(分层 · 可证伪 · 可度量)

**真实目标:** 开源浏览器内核的 SOTA——在 **compat-per-LOC**、**机制优雅度**、**性能/复杂度比**、**底层受欢迎度**四个维度综合第一,理论功能覆盖与网站兼容逼近 99%,但**手写内核保持常数级、机制级**。

### 三条硬胜利条件(对外可比 · 每提交公开)

| # | 指标 | 胜利定义 |
|---|------|----------|
| 1 | **compat-per-LOC 碾压** | 官方 WPT 子集通过数 / 手写行数,比 Chromium 高 **≥ 1 个数量级**。唯一硬指标,公开记分牌。 |
| 2 | **覆盖逼近** | 沿"逐 9"阶梯推进:A 档(静态文档)→ B 档(~80-90% 主流站)→ C 档(99%+),每档绑定官方 WPT 子集阈值,**通过率只进不退**(CI 强制)。 |
| 3 | **机制纯度** | 手写面分档封顶(A 档 < ~5 万行 / B 档 < ~15 万行),且**零处**绕过 IR 边界 / 手动标脏 / 静默 stub——物理保证。覆盖面靠生成,不靠手写膨胀。 |

### 两条软胜利条件(最终的"综合超越")

- **底层受欢迎度。** 成为该生态位事实上的参考实现——被课程、博客、其他项目当作"想读懂浏览器就读它"的首选。
- **机制被引用。** 我们的增量内核 / Platform-as-Data / IR 边界设计,成为其他项目借鉴的范式。

---

## 当前位置(诚实标定)

地基已立,骨架全绿:六大支柱、反 v0 四类 bug 的宪法守卫、一条端到端竖线(`<div>hello</div>` → PNG)、全套 CI(类型检查 + lint + 宪法 + 测试)绿。

但这是**"能编译可运行的最小实现",离真能渲染网站还很远**:

- Platform-as-Data 的第一刀已经打通:级联生成器已从数据表 emit `position` / `float` / `flex-direction` / `grid-template-columns` / `opacity` / `transform` / `z-index` / padding / border / overflow 等属性,真实文档可以触发 flex / grid / table / float / positioned layout 和合成层。
- 文本已经能通过内置 glyph coverage 路径画出像素,但**还没有接入生产级 HarfBuzz / FreeType**;复杂脚本、真实字体指标、字体 fallback 仍是后续战役。
- WPT 仍是 curated/self-test 子集加导入工具,**不是官方全套 WPT**,所以通过率还不是对外可比的浏览器兼容数字。

我们完成了"骨架先行",也证明了第一批"机制填肉"可以端到端闭环。下一步不是堆功能,而是把每一块肉继续做成机制级能力,绝不退化成 Chromium 式的手写堆叠。

---

## 通往 SOTA 的前三个机制级战役

从"能运行"到"对外可比":

1. **扩大战果:Platform-as-Data 从"属性 emit"推进到"语法/简写/行为闭环"。** 第一批布局与合成属性已能驱动真实文档;下一刀是让更多 shorthand、真实 track-list、transform 函数、字体/文本长尾等继续以数据行 + 通用机制增长,而不是落回手写分支。
2. **接入生产级整形 + 栅格化。** 在已有的 `TextShaper` / glyph coverage / `PaintBackend` 接缝上接 HarfBuzz / FreeType,把内置像素路径升级为真实字体、复杂脚本、fallback 和字形缓存——证明复用边界的设计是对的。
3. **接入官方 WPT。** 拿到第一个**可与 Chromium 直接对比**的 compat-per-LOC 数字——把"超越 Google"从口号变成记分牌上的数。

---

> **我们证明:在最优解的机制设计面前,小赢大。**
