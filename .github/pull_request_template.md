# Pull Request

## What changed

- 

## Evidence

Check every command that matches the change. Paste the key result lines below.

- [ ] `npm run typecheck`
- [ ] `npm run lint`
- [ ] `npm run test`
- [ ] `npm run wpt:subsets`
- [ ] `npm run wpt:subsets -- --trace`
- [ ] `npm run benchmark`
- [ ] `npm run evidence`
- [ ] Targeted command:

Key result lines:

```text

```

## Compatibility / WPT

- Change type:
- Labels:
- Linked good-first task:
- Linked RFC:
- WPT files or subset manifests touched:
- Baseline pass count before:
- Baseline pass count after:
- Failure bucket if still partial:

## Architecture Check

- [ ] No silent stubs or fake browser APIs.
- [ ] No cross-stage internal imports.
- [ ] No manual stale/invalidation API.
- [ ] Geometry still flows through `FragmentTree` only.
- [ ] Public evidence updated when LOC, feature counts, trace counts, resources, smoke scenarios, or WPT outcomes changed.

## Notes For Reviewers

- 
